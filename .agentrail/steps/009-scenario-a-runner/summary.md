Scenario A end-to-end runner: cli::run wires together Config -> validate -> assemble (memoized) -> LoadPlan -> spawn cor24-run -> capture UART -> check expectations. Four cli helpers (run_scenario, assemble_artifacts, run_emulator, check_expectations) cover the orchestration. Two end-to-end integration tests against real cor24-run pass. 60 tests total green. .gitignore now excludes .sw-launch/ build outputs. Phase 1 functionally complete.