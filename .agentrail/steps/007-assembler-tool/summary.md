Assembler + listing parser: src/tool.rs combines cor24-run --assemble wrapper (new/from_path/build with in-process memoization keyed on tool_path + sha256(input) + extra_args) and Listing parser (handles leading-whitespace label lines, addr:bytes data lines, trailing BSS labels via end-of-data fallback). cli::build wired up to assemble all assembler-kind layers in scenario, writing under .sw-launch/build/. 53 tests green including 1 real-cor24-run integration test. Module count back to 7 by combining listing into tool.rs (re-exported as sw_launcher::listing for callers).