Layer.kind dispatch in assemble_artifacts: assembler->Tool(Assembler), pcode->Tool(Pcode) lazy-initialized, binary/pcode-image->pass-through (input is the artifact). build_scenario rewritten to call shared assemble_artifacts. New dispatch fixture (asm + pcode + binary layers) and integration test gated on both cor24-run and pa24r. 71 tests green. assemble_artifacts hits Function LOC cap (88 vs 50); same root constraint conflict, documented as 7th trade-off.