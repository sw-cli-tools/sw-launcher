Phase 4 step 5: sw-launch graph <scenario> renders the layer DAG as text or JSON. Edges come from cross-layer patches (target/value of shape <layer>.<symbol>); implicit declaration-order edges keep the tree visible when no patches exist. Pure presentation logic over Config -- no tools spawned. New src/graph.rs (~170 LOC, 6 fns); hand-rolled JSON serializer to avoid serde_json dep. 4 new graph_cli tests + 1 stub replacement in tests/cli.rs. tests/cli_unit.rs's dispatch_returns_not_implemented test deleted (no remaining NotImplemented subcommands after step 5). 117 tests / 25 binaries pass; sw-checklist 14 fail (unchanged from step 4 -- graph.rs's 6 fns under the 7-fn cap, Crate Module Count already 8 from doctor.rs and stays).