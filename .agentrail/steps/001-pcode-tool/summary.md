PcodeAssembler + SourceSpec resolution: unified Tool struct with ToolKind::Assembler|Pcode dispatches argv (cor24-run --assemble vs pa24r -o), SourceSpec covers Path/FromPath/Sibling resolution kinds, Tool::from_source(spec, config_dir, kind) replaces from_path(). Real-pa24r integration test (gated on PATH) assembles hello.spc successfully. 65 tests green. 4th documented sw-checklist trade-off (build LOC 53>50) joins the 3 inherited; same root constraint conflict between Function LOC + Function Count + Crate Module Count caps.