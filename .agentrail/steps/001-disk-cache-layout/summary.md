Phase 4 step 1: replaced in-process HashMap cache with content-addressed disk cache under $SW_LAUNCH_CACHE_DIR / $XDG_CACHE_HOME / ~/.cache; same step extracted sw-launcher-tool sub-crate to begin the documented sub-crate split (Cargo.toml workspace + crates/sw-launcher-tool/{tool,cache}.rs; main crate re-exports preserve sw_launcher::tool::* paths). Cache::get_or_fill uses File::lock for serialization, atomic temp-dir-rename for commit, and provenance.toml sha verification for corruption detection. 5 cache_unit tests + 1 cache_persist integration test (verifies created_unix unchanged across two sw-launch run invocations). 92 tests / 20 binaries pass; sw-checklist 15 pass / 9 fail (same count as Phase 3, failures shifted into the new sub-crate's tool.rs as documented).