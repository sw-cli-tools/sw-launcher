ToolKind::PcodeLinker landed. assemble_artifacts pcode arm now runs pa24r AND p24-load (lazy-init, layer.load.address required for linker --load-addr). tests/scenario_b.rs simplified: drops manual p24-load, uses kind=pcode, sw-launch handles the chain. New tool_unit test verifies PcodeLinker memoization with extra_args distinct cache keys. 76 tests green. 2 new sw-checklist trade-offs (assemble_artifacts 120 LOC, cli.rs 525 LOC); same root constraint conflict, Phase 5 sub-crate extraction is the documented remediation.