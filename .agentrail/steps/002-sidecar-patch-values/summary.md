sidecar:<path> patch value form: manifest reads a hex literal from a build-time text file (relative to config_dir or absolute), with or without 0x prefix. Threaded config_dir into LoadPlan::build (signature change rippled through cli + 7 existing tests). Three new tests cover the happy path, missing sidecar (E0019), and non-hex contents. 79 tests green. resolve_patch_term grew to 62 LOC; one new sw-checklist Function-LOC trade-off, same root constraint.