E0003 overlap check implemented in validate (was deferred in step 006). Walks scenario layers, collects (layer, region, start, end) for binary-kind layers with explicit size + non-embedded segments with explicit load.address+size; pairwise overlap fires E0003 with both layer names + ranges. New manifest test verifies reserved heap appears in segments not loads. New validate test asserts E0003 fires for a deliberate binary+heap overlap. 81 tests green; same 9 documented sw-checklist trade-offs.