Phase 5 step 1: extracted config + validate into sub-crates (sw-launcher-config + sw-launcher-validate). ErrorCode moved to sw-launcher-config so it can be shared without a circular dep. Main crate's lib.rs re-exports both as pub use sw_launcher_config as config and pub use sw_launcher_validate as validate, so all existing call sites and tests keep working. From<anyhow::Error> for Error already in place from Phase 4 step 1 handles the boundary. 117 tests / 26 binaries pass; sw-checklist 26 pass / 14 fail (same total as Phase 4 close, but Crate Module Count [sw-launcher] dropped -- the validate fails moved cleanly into the sub-crate's budget where they belong; step 2 will drop the manifest fn count fail similarly).