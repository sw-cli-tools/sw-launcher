[package]
name = "sw-launcher-config"
version = "0.1.0"
edition = "2024"
description = "Manifest types and stable diagnostic codes for sw-launcher"
license = "MIT"
repository = "https://github.com/sw-cli-tools/sw-launcher"

[lib]
name = "sw_launcher_config"
path = "src/lib.rs"

[dependencies]
anyhow = { workspace = true }
camino = { workspace = true }
serde = { workspace = true }
toml = { workspace = true }
