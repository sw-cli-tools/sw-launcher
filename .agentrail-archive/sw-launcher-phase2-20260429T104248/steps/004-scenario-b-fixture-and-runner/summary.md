Scenario B end-to-end: tests/scenario_b.rs runs sw-launch against pre-built pvm.bin + hello.p24m + code_ptr patch; UART output 'PVM OK\\nHello\\nHALT' confirms the loop. Two tests (happy + misaligned-patch) gated on cor24-run + pa24r + p24-load + sibling pvm.s. 73 tests total green. The pa24r->p24-load chain runs in the test fixture rather than in sw-launch itself; pcode-linker integration deferred to Phase 3 step k. No new sw-checklist failures.