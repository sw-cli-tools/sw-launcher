Scenario C end-to-end: pvm.bin@0 + ocaml.p24m@0x040000 + sidecar-resolved code_ptr/heap_limit patches + .ml source via UART with EOT. Real OCaml interpreter evaluates 'let x = 1 + 2 in print_int x' and emits '3' on UART; happy path test asserts contains('3'). One-line validate fix: check_patch_term now recognizes 'sidecar:' prefix (was being misread as a layer reference). Oversized-source negative test fires E0004. 86 tests green; same 9 documented sw-checklist trade-offs.