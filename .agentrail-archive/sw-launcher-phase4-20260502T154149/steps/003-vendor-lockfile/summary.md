Phase 4 step 3: shipped sw-launch.lock and the vendor sync/status subcommands. New crates/sw-launcher-lockfile sub-crate with Lockfile {read, read_optional, write, to_toml_string, status}, EntryStatus {Fresh, Drifted, Unresolvable, Unverified}, sync, and update_only_drifted that preserves fresh entries' synced_at. CLI: vendor sync walks the manifest's layers + sidecar paths and writes sw-launch.lock atomically (temp + rename, sorted by key); vendor status reads it and exits non-zero on drift (E0041). sw-launch run/build now call enforce_lockfile pre-flight; --update-lock bypasses. Three new error codes E0040 (lockfile missing), E0041 (drift), E0042 (unresolvable path). vendor_repo / vendor_sha auto-populated from .git/HEAD when present. now_iso8601 uses Hinnant's days-to-civil so dates stay correct without pulling in chrono. 10 new tests (5 lockfile unit + 5 vendor_cli integration). 109 tests / 23 binaries pass; sw-checklist 12 fail (was 11; +1 for sw-launcher-lockfile lib.rs fn count, same root constraint).