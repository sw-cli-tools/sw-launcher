Phase 4 step 4: sw-launch doctor checks the host (cor24-run --version, pa24r/p24-load on PATH, cache dir writable) and optionally a manifest (every layer.input + sidecar resolves; sw-launch.lock fresh via Lockfile::status). Output is human-readable by default; --json emits a structured array. Exit 0 iff every check is PASS or WARN; any FAIL exits non-zero. New src/doctor.rs (Option A from step plan) keeps the doctor in-crate as a thin orchestrator over Cache, Lockfile, Tool::find_binary_on_path. Drive-by fix: tests/scenario_c.rs's tmp_dir had a pre-existing race -- both tests shared the same dir name -- exposed when test interleavings shifted; fixed via AtomicU64 counter suffix. 5 new doctor_cli tests + 1 stub replacement; 114 tests / 24 binaries pass; sw-checklist 14 fail (was 12; +2 from doctor.rs Module Function Count + Crate Module Count back to 8 -- same root constraint, addressed by Phase 5).