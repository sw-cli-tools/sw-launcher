Phase 4 step 2: shipped sw-launch cache list / explain / clean subcommands. cache.rs gained read-side API (list returning Vec<CacheListing> sorted by digest, find_by_prefix with explicit zero/ambiguous-match errors, clean(predicate, dry_run) returning what was removed). Provenance gains an optional layer_name (informational only; not part of digest); Tool::build_via_disk passes BuildJob.layer_name through new Cache::get_or_fill_with. CLI handlers cache_list (table + --json), cache_explain (full provenance + paths), cache_clean (--older-than 7d/12h/60m, --all, --dry-run). 7 cache_cli integration tests pre-seed a tempdir cache and exercise every subcommand. 3 prior stub tests in tests/cli.rs replaced with empty-cache happy-path smoke tests. 99 tests / 21 binaries pass; sw-checklist 11 fails (was 9 -- +2 documented as same root-constraint, addressed by Phase 5 sub-crate split).