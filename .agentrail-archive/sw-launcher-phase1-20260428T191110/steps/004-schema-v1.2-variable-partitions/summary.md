Schema v1.2: heap-analysis.md (per-repo categorization with historical benchmarks and shrinkage backlogs); design.md addressing model replaces v1.1 fixed grid with named [memory_profiles.<name>] blocks (compiled-app/interpreter-only/repl-inline-compile/compiler-image/resident-shell, sized 64 KiB to 512 KiB total); heap_justification required for >32 KiB claims with 5-category enum; --strict rejects dead-leak/algorithmic-bloat categories; validation codes E0028-E0034 appended