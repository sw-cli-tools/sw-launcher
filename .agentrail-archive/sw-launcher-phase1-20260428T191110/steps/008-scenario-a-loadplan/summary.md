LoadPlan + cor24-run argv emitter: src/manifest.rs translates Config + scenario + Artifacts into a deterministic plan (memory loads sorted address-ascending, UART chunks in scenario order, patches sorted, embedded segments contribute to segments but not memory loads). cor24_argv emits --load-binary/--patch/--entry/--uart-input with EOT/ETX/EOF terminator literal bytes. 5 snapshot+ordering tests; 58 total tests green. Crate-module count is now 8 (max 7) - documented as same trade-off as step 006's validate.rs splits.